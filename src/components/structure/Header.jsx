import React from 'react'
import { Sidebar, Topbar } from '../.'

const Header = () => {
     return (
          <header>
               <Sidebar />
               <Topbar />
          </header>
     )
}

export default Header